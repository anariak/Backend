//Modelos
const vacaciones = require('../models/vacaciones')
const permiso = require('../models/permisos')
const horasExtra = require('../models/hora_extra')

module.exports = getSolicitudes=(req, res) =>{
    if(req.params.id != null){
        const 
    }
}

